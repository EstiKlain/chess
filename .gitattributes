# Normalize all text files to LF in the repo, regardless of the
# checkout platform's line-ending convention. Matters beyond style once
# the project builds/runs inside a Linux container: a CRLF shebang line
# in any future shell script/Dockerfile fails with "exec format error".
* text=auto eol=lf
